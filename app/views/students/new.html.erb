<h1>Student Form</h1>
<%= form_tag new_students_path %>
    <label>First Name:</label>
    <%= text_field_tag name='student[first_name]' %>
    <label>Last Name:</label>
    <%= text_field_tag name='student[last_name]' %>
    <%= submit_tag "Create Student" %>
<% end %>